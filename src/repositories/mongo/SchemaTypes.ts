export const RequiredString = {
  type: String,
  required: true,
};

export const OptionalString = {
  type: String,
  required: false,
};

export const RequiredBoolean = {
  type: Boolean,
  required: true,
};
